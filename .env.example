# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Supertone Configuration  
SUPERTONE_API_KEY=your_supertone_api_key_here

# Database Configuration (if using PostgreSQL)
# DATABASE_URL=postgresql://username:password@localhost:5432/ai_english_tutor

# Server Configuration
PORT=5000
NODE_ENV=development

# WARNING: API 키를 넣기 전까지는 절대 외부 모델 호출 금지
# DO NOT use real API keys until you're ready to incur charges
# These services will charge you for each API call:
# - OpenAI: GPT-4o, DALL-E 3 image generation
# - Supertone: Text-to-speech generation

# Cost estimates (as of 2024):
# - OpenAI GPT-4o: ~$0.005-0.015 per 1K tokens
# - OpenAI DALL-E 3: ~$0.04 per image
# - Supertone TTS: ~$0.20 per 1K characters
